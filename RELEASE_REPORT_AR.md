# PS Cafe Manager — V6.2.0 Owner & Branding Secure Foundation

## ما تم تنفيذه

- استعادة هوية التطبيق بصورة الشخص المرسلة في شاشة الدخول والهيدر وجميع أيقونات PWA بالمقاسات الصحيحة.
- إخفاء رقم الإصدار والتفاصيل الفنية من واجهة العامل اليومية.
- تحويل الإعدادات إلى **إدارة النشاط** ولا تظهر إلا لصاحب النشاط.
- تقييد تبويب المستخدمين لصاحب النشاط فقط، مع أدوار: صاحب النشاط، مدير، كاشير، مشاهدة فقط.
- ترقية حساب Admin القديم تلقائيًا إلى Owner محليًا حتى لا يفقد المالك الدخول بعد التحديث.
- إضافة روابط شروط الاستخدام والخصوصية والملكية/الدعم في شاشة الدخول بطريقة بسيطة.
- إضافة لوحة بيانات النشاط ونسخ احتياطي للمالك.
- وضع التثبيت، الإصدار، الكاش، والتنبيهات داخل قسم دعم فني مغلق للمالك فقط.
- إضافة زر **اختبار تنبيه الآن** داخل الأدوات الفنية.
- استبدال مزامنة `allow_all` القديمة بأساس آمن يعتمد على Supabase Auth + Business ID + RLS.
- عدم رفع كلمات مرور مستخدمي التطبيق المحليين إلى بيانات التشغيل السحابية.
- إضافة ملف `SUPABASE_SETUP.sql` ليُنفّذ داخل مشروع Supabase الذي يملكه صاحب النشاط.

## مهم قبل الربط بالسحابة

التطبيق يعمل محليًا بعد الرفع كما كان. الربط السحابي لن يعمل إلا بعد أن ينشئ صاحب النشاط مشروع Supabase، وينشئ مستخدم المالك في Supabase Auth، ثم ينفذ ملف SQL ويضع Project URL وPublishable Key وBusiness ID وبيانات حساب Supabase Auth المصرح به للجهاز داخل لوحة إدارة النشاط. جهاز المالك يستخدم حساب owner، وأجهزة العاملين تستخدم حسابات محدودة يضيفها المالك لاحقًا.

## لم يتم تغييره عمدًا

- لم أغير حساب رسوم الجلسات أو المباريات أو الفواتير أو المصروفات.
- لم أنفذ خزنة/ورديات/طرق دفع جديدة في هذا الإصدار حتى لا نمس المنطق المالي قبل تدقيقه منفصلًا.

## ملفات الرفع إلى GitHub

ارفع جميع الملفات الموجودة في ZIP إلى جذر المشروع بجانب `index.html`. لا تعدّل ملف workflow الحالي إن كان النشر يعمل بالفعل.
